import React from 'react';
import { compose } from 'recompose';
import Navigator from '../../Services/Navigator';

const App = () => [<Navigator key={'app-navigator'} />];

export default compose(App);
